"""The English mirror of the site copy. content_zh.py is the primary; build.py
checks that both sides carry the same chapters, section ids and block shapes."""

VERSION = "2.0.2"
REPO = "https://github.com/doggy8088/mgit"

SITE = {
    "code": "en",
    "base": "en/",
    "lang": "en",
    "hreflang": "en",
    "og_locale": "en_US",
    "short": "EN",
    "version": VERSION,
    "skip": "Skip to content",
    "nav_label": "Main",
    "language_label": "Language",
    "theme_dark": "Switch to the dark theme",
    "theme_light": "Switch to the light theme",
    "og_image_alt": "mgit terminal output: one command sweeping six Git repositories, closing with a one line summary.",
    "nav": [
        {"id": "playground", "label": "Playground", "href": "en/playground/"},
        {"id": "docs", "label": "Docs", "href": "en/docs/"},
        {"id": "install", "label": "Install", "href": "en/docs/install/"},
        {"id": "github", "label": "GitHub", "href": REPO, "external": True},
    ],
    "footer": {
        "columns": [
            {
                "title": "Start here",
                "links": [
                    {"label": "Install and verify", "href": "en/docs/install/"},
                    {"label": "Commands and options", "href": "en/docs/usage/"},
                    {"label": "Playground", "href": "en/playground/"},
                    {"label": "Recipes", "href": "en/docs/recipes/"},
                ],
            },
            {
                "title": "Behaviour",
                "links": [
                    {"label": "How repositories are found", "href": "en/docs/discovery/"},
                    {"label": "Output, colour and glyphs", "href": "en/docs/output/"},
                    {"label": "Exit codes and automation", "href": "en/docs/exit-codes/"},
                    {"label": "Environment and platforms", "href": "en/docs/environment/"},
                ],
            },
            {
                "title": "Source and releases",
                "links": [
                    {"label": "GitHub repository", "href": REPO},
                    {"label": "Releases and checksums", "href": REPO + "/releases"},
                    {"label": "Changelog", "href": REPO + "/blob/main/CHANGELOG.md"},
                    {"label": "npm: @willh/mgit", "href": "https://www.npmjs.com/package/@willh/mgit"},
                ],
            },
        ],
        "legal": "MIT licensed © 2026 Will Huang. mgit is an independent project and is not affiliated with the Git project.",
        "built": "No analytics, no cookies, no third party requests. The playground runs entirely in your browser tab.",
    },
    "labels": {
        "copy": "Copy",
        "copied": "Copied",
        "copy_failed": "Press Ctrl+C",
        "install_tablist": "Installation method",
        "anchor_label": "Link to this section",
        "input_label": "Type a command for the playground",
        "input_placeholder": "Try mgit --help, or press Tab to complete",
        "screen_label": "Playground terminal output",
        "stop": "Stop",
        "terminal_title": "Sandbox terminal — everything happens in this tab",
        "terminal_fallback": "This terminal needs JavaScript. Nothing else on the site does: the install commands, the option tables and every documentation page work without it.",
        "copy_transcript": "Copy all",
        "reset": "Reset",
        "share": "Copy link",
        "scenario": "Working directory",
        "target": "Where the output goes",
        "simulate": "Simulate",
        "no_git": "Remove git",
        "animate": "Play back",
        "workspace": "Working directory",
        "add_repo": "Add a repository",
        "add": "Add",
        "inspector": "What it did",
        "inspector_empty": "Run an mgit command and this panel lists every directory it opened, every git call it made, and how those results became one exit code.",
        "hint_run": "run",
        "hint_complete": "complete",
        "hint_history": "history",
        "hint_interrupt": "interrupt",
        "hint_clear": "clear",
        "exit": "exit",
        "welcome": "Type a command, or pick one below. Everything runs in this tab and nothing touches your files.",
        "scenario_loaded": "Loaded working directory: ",
        "target_changed": "Output target: ",
        "git_gone": "git has been taken off PATH. Every run now ends in 127.",
        "git_back": "git is back on PATH.",
        "reset_done": "The working directory is back the way it started.",
        "interrupt_note": "Ctrl+C reaches every git that mgit started, so the run stops at once and reports 130.",
        "name_rule": "Use letters, digits, dots, dashes or underscores.",
        "name_taken": "That name is already taken.",
        "here_is_repo": ". (this directory)",
        "unreadable": "unreadable",
        "dot_git_file": "file",
        "summary": "{repos} repositories at depth 1",
        "state_clean": "clean",
        "state_dirty": "changes",
        "state_behind": "behind",
        "state_ahead": "ahead",
        "state_blocked": "will fail",
        "action_dirty": "edit",
        "action_behind": "behind",
        "action_block": "block",
        "action_remove": "remove",
        "action_title_dirty": "Toggle an uncommitted change",
        "action_title_behind": "Toggle whether the remote has new commits",
        "action_title_block": "Toggle whether pull fails here",
        "action_title_remove": "Remove from the working directory",
        "command": "The command",
        "search_root": "Searched",
        "depth": "Depth",
        "git_args": "Handed to git",
        "colour": "Colour",
        "glyphs": "Glyphs",
        "mode": "Mode",
        "on": "on",
        "off": "off",
        "exit_code": "Exit code",
        "discovery": "The walk",
        "git_runs": "git, once per repository",
        "result": "How that became one exit code",
        "not_modelled": "not modelled in the sandbox",
        "total": "Repositories",
        "succeeded": "Succeeded",
        "failed": "Failed",
        "skipped": "Skipped",
        "summary_stream": "Summary went to",
        "summary_none": "nowhere: nothing failed and --summary was not given",
        "interrupted": "Interrupted",
        "interrupted_note": "Ctrl+C stops the whole run at once",
        "step_scan": "read",
        "step_repository": "repository",
        "step_descend": "descend",
        "step_depth-limit": "depth limit",
        "step_already-visited": "already visited",
        "step_skip-file": "not a directory",
        "step_unreadable": "unreadable",
        "exit_ok": "everything succeeded",
        "exit_failure": "something failed, or no repository was found",
        "exit_usage": "the command line could not be parsed",
        "exit_notFound": "git could not be started",
        "exit_git": "from the first failing git command",
        "exit_interrupted": "interrupted with Ctrl+C",
        "exit_brokenPipe": "the reader closed the pipe",
        "depth_title": "What --depth actually reaches",
        "depth_label": "Depth",
        "depth_found": "{n} repositories found",
        "depth_tree": "Working directory",
        "depth_output": "Output of mgit --list",
        "stream_title": "What lands in the file, and what stays on screen",
        "stream_file": "Inside the file:",
        "stream_screen": "Still on screen",
        "note_redirect": "{stream} went into {path} ({lines} lines). Read it back with `cat {path}`.",
        "note_sigpipe": "head closed the pipe after {count} lines. The real mgit restores the default SIGPIPE behaviour, so it stops quietly instead of printing a panic, and the exit code on this line belongs to head rather than to mgit.",
        "note_unmodelled": "The sandbox does not model the output of {command}. On a real run mgit still executes it in every repository and collects its exit code.",
        "note_unknown_command": "This sandbox knows mgit, plus {commands}. Type help for the full list.",
        "noscript": "This page needs JavaScript: the playground runs entirely in your browser. Every documentation chapter, option table and install command works without it.",
        "install_verify": "Check the install:",
    },
}

SCENARIOS = {
    "workbench": {
        "name": "An ordinary working directory (6 repositories)",
        "blurb": "Six repositories: one with an uncommitted change, one three commits behind its remote, one linked worktree, one on a hotfix branch.",
    },
    "nested": {
        "name": "A second level",
        "blurb": "vendor/ holds two more repositories, and web-console holds one too. mgit never walks into a repository, so that last one is never counted.",
    },
    "worktrees": {
        "name": "Worktrees and submodules",
        "blurb": "These directories keep their .git as a file rather than a directory. mgit accepts both, so worktrees and submodule checkouts are found.",
    },
    "links": {
        "name": "Symbolic links",
        "blurb": "gateway is a link to api-gateway. Both names are run, the real directory is only walked once, and a link loop cannot spin forever.",
    },
    "empty": {
        "name": "No repository at all",
        "blurb": "See what mgit says and returns when it finds nothing, and exactly what --allow-empty changes.",
    },
    "inside-repo": {
        "name": "You are standing inside a repository",
        "blurb": "When the current directory is itself a repository and holds no others, mgit behaves exactly like plain git.",
    },
    "locked": {
        "name": "One directory cannot be read",
        "blurb": "restricted/ denies permission. mgit warns on stderr and finishes the rest of the walk.",
    },
    "fleet": {
        "name": "Twelve repositories",
        "blurb": "The one to use for the play back, for --fail-fast, and for pressing Ctrl+C in the middle of a run.",
    },
}

TARGETS = [
    {"id": "unix-tty", "name": "macOS / Linux terminal"},
    {"id": "pipe", "name": "Redirected to a file or a CI log"},
    {"id": "windows-terminal", "name": "Windows Terminal / PowerShell 7"},
    {"id": "windows-legacy", "name": "Legacy Windows console (not UTF-8)"},
]

INSTALL_ITEMS = [
    {
        "label": "macOS / Linux",
        "command": "curl -fsSL https://raw.githubusercontent.com/doggy8088/mgit/main/install.sh | sh",
        "note": "The installer detects your operating system and CPU, downloads the matching archive and **verifies its SHA-256 before installing anything**.",
    },
    {
        "label": "Windows",
        "command": "irm https://raw.githubusercontent.com/doggy8088/mgit/main/install.ps1 | iex",
        "note": "The same flow in PowerShell. Works in Windows PowerShell 5.1 and in PowerShell 7+.",
    },
    {
        "label": "npm",
        "command": "npm install -g @willh/mgit",
        "note": "The package bundles the official binaries for six platforms and runs **no install scripts**; `npx @willh/mgit` works without installing.",
    },
    {
        "label": "From source",
        "command": "cargo install --path .",
        "note": "Needs Rust 1.85 or newer. `cargo build --release` produces `target/release/mgit`.",
    },
]

LANDING = {
    "title": "mgit — one command for every Git repository in the directory",
    "description": "mgit runs the same git command in every Git repository under the current directory and collects the results into one report and one exit code. A single Rust binary with no runtime dependencies. Try it in your browser.",
    "hero": {
        "eyebrow": "One Rust binary · macOS · Linux · Windows",
        "h1": "One command for every Git repository in the directory",
        "lede": "With no arguments it is `git status -s` in every repository. `mgit pull`, `mgit fetch --all` and `mgit log -n 1` work the same way. Clean repositories stay quiet, and failures are collected into one exit code.",
        "primary": {"label": "Try it in your browser", "href": "en/playground/"},
        "secondary": {"label": "Read the docs", "href": "en/docs/"},
        "facts": [
            "One binary, no runtime dependencies: no Python, no Node.js, no package manager.",
            "Every git argument is passed as an argument vector, never through a shell, so nothing is injected or glob expanded.",
            "Releases ship SHA-256 checksums and the official installers verify them before touching your PATH.",
        ],
    },
    "demo": {
        "title": "You can actually type here: this is mgit's own parsing and discovery running in your browser",
        "note": "The sandbox holds six repositories, and `web-console` has an uncommitted change. Try `mgit pull`.",
        "chips": ["mgit", "mgit --summary", "mgit pull", "mgit --list", "mgit --help"],
        "auto": "mgit --summary",
        "more": "Open the full playground",
    },
}

LANDING["sections"] = [
    {
        "id": "loop",
        "eyebrow": "Why it exists",
        "h2": "The loop you stop writing",
        "lede": "An application, a shared package, some examples, the deployment scripts and a docs site, each its own repository, sitting side by side in one working directory and referring to each other by relative path, with no `git submodule` holding them together. That is the situation mgit was written for.",
        "blocks": [
            ("pre", "# before\nfor d in */; do (cd \"$d\" && git status -s); done\n\n# and a second version for PowerShell, which behaved slightly differently"),
            ("p", "One command instead, and the same binary with the same test suite on all three operating systems:"),
            ("cmd", "mgit"),
            ("p", "It does not change the contents of any repository and it does not manage the relationships between them. Use submodules when you need to pin versions; a monorepo is the answer when you need one CI pipeline and one release."),
        ],
    },
    {
        "id": "claims",
        "eyebrow": "What it is",
        "h2": "One executable, nothing else",
        "lede": "",
        "cards": [
            {
                "meta": "8 release targets",
                "title": "A single Rust binary",
                "text": "macOS, Linux and Windows share the same code and the same tests. The Bash and PowerShell scripts of 1.0 are archived.",
            },
            {
                "meta": "No runtime dependencies",
                "title": "Download and run",
                "text": "No Python, no Node.js, no package manager. The npm package bundles the official binaries for six platforms and has no postinstall.",
            },
            {
                "meta": "Verifiable",
                "title": "Verified before it is installed",
                "text": "Every archive ships a SHA-256 checksum and the official installers check it first. Every git argument is passed as an argument vector, never through a shell.",
            },
            {
                "meta": "Does not reinvent git",
                "title": "A transparent proxy",
                "text": "mgit only recognises the options in its own documentation. Everything it does not know (`-c`, `--git-dir`, …) is handed to git untouched.",
            },
        ],
    },
    {
        "id": "find",
        "eyebrow": "Discovery",
        "h2": "It only touches directories you can see",
        "lede": "By default it looks one level down, and it never walks into a repository it has already found. Drag the slider to see what `--depth` actually reaches.",
        "component": "depth",
        "after": [
            ("p", "The full set of six rules, including worktrees, submodules, symbolic links and ordering, is in [how repositories are found](en/docs/discovery/)."),
        ],
    },
    {
        "id": "report",
        "eyebrow": "Output",
        "h2": "A report you can put in a script",
        "lede": "The summary goes to stderr by default, so `mgit status -s > status.txt` leaves you clean data in the file while failures stay visible on screen.",
        "component": "streams",
        "after": [
            ("table", "Exit codes", ["Code", "Meaning"], [
                ["`0`", "every repository succeeded"],
                ["`1`", "a repository failed, or no repository was found"],
                ["`2`", "the command line could not be parsed"],
                ["`127`", "git could not be started"],
                ["`130`", "interrupted with Ctrl+C"],
                ["anything else", "the exit code of the first failing git command"],
            ]),
        ],
    },
    {
        "id": "install",
        "eyebrow": "Install",
        "h2": "One line, verified before it installs",
        "lede": "The installer detects your operating system and CPU, downloads the matching archive and verifies its SHA-256 before installing.",
        "component": "install",
        "after": [
            ("p", "Manual downloads, checksum verification and building from source are in [install and verify](en/docs/install/)."),
        ],
    },
]

DOCS_INDEX = {
    "title": "mgit documentation",
    "description": "Everything about mgit: installing and verifying it, the command line, how repositories are found, the output format, exit codes, environment variables, platform behaviour and recipes.",
    "h1": "Documentation",
    "lede": "Every chapter stands on its own, and every rule in them can be tried in the playground with one click.",
}

DOC_CARDS_INTRO = "Seven chapters, each one readable on its own."

CHAPTERS = [
    {
        "id": "install",
        "nav": "Install and verify",
        "title": "Install and verify",
        "description": "Install mgit on macOS, Linux and Windows: one line, npm, a manual download with SHA-256 verification, or a build from source.",
        "lede": "Four ways in, all of them ending at the same thing: one binary with no runtime dependencies.",
        "blocks": [
            ("h2", "one-line", "One line"),
            ("p", "The official installers detect the operating system and CPU architecture, download the matching archive, **verify its SHA-256 before installing**, and tell you when the install directory is missing from your `PATH`."),
            ("cmd", "curl -fsSL https://raw.githubusercontent.com/doggy8088/mgit/main/install.sh | sh", "macOS and Linux (POSIX sh)."),
            ("cmd", "irm https://raw.githubusercontent.com/doggy8088/mgit/main/install.ps1 | iex", "Windows PowerShell 5.1 and PowerShell 7+ (which also works on macOS and Linux)."),
            ("note", "Want to read the script first?", "Open the URL in a browser or save it to a file. Those two URLs are `install.sh` and `install.ps1` from the repository; there is no second copy."),
            ("h2", "verify", "Verify the install"),
            ("p", "Check the version, and check that `PATH` really points at the binary you just installed:"),
            ("pre", "$ mgit --version\nmgit 2.0.2\n\n$ command -v mgit\n/usr/local/bin/mgit"),
            ("h2", "npm", "npm and npx"),
            ("p", "Requires Node.js 20 or newer. The package already contains the official binaries for six platforms and runs no code at install time (**there is no postinstall**)."),
            ("cmd", "npm install -g @willh/mgit", "pnpm and yarn work too; `npx @willh/mgit` and `bunx @willh/mgit` run it without installing."),
            ("h2", "manual", "Download and verify by hand"),
            ("p", "Take the archive for your platform from [GitHub Releases](https://github.com/doggy8088/mgit/releases). Each one ships a matching `.sha256`, and every checksum is also collected in `SHA256SUMS.txt`."),
            ("table", "8 release targets", ["Platform", "Archive"], [
                ["Linux x86_64 (musl, static)", "`mgit-x86_64-unknown-linux-musl.tar.gz`"],
                ["Linux aarch64 (musl, static)", "`mgit-aarch64-unknown-linux-musl.tar.gz`"],
                ["Linux x86_64 (glibc)", "`mgit-x86_64-unknown-linux-gnu.tar.gz`"],
                ["Linux aarch64 (glibc)", "`mgit-aarch64-unknown-linux-gnu.tar.gz`"],
                ["macOS Apple Silicon", "`mgit-aarch64-apple-darwin.tar.gz`"],
                ["macOS Intel", "`mgit-x86_64-apple-darwin.tar.gz`"],
                ["Windows x64", "`mgit-x86_64-pc-windows-msvc.zip`"],
                ["Windows on ARM", "`mgit-aarch64-pc-windows-msvc.zip`"],
            ]),
            ("pre", "# macOS\nshasum -a 256 -c mgit-aarch64-apple-darwin.tar.gz.sha256\n\n# Linux\nsha256sum -c mgit-x86_64-unknown-linux-musl.tar.gz.sha256"),
            ("h2", "source", "From source"),
            ("p", "Rust 1.85 or newer is required; the `rust-version` is declared and CI checks it."),
            ("pre", "git clone https://github.com/doggy8088/mgit.git\ncd mgit\ncargo build --release      # target/release/mgit\ncargo install --path .     # or install into ~/.cargo/bin"),
            ("h2", "upgrade", "Upgrading and removing"),
            ("p", "Upgrading is the same command again: the installer replaces the old binary. Removing it is deleting that one file, because mgit leaves nothing else anywhere."),
            ("ul", [
                "`MGIT_VERSION=2.0.1` installs a specific version.",
                "`MGIT_INSTALL_DIR` chooses the install directory.",
                "For the npm package, `npm uninstall -g @willh/mgit`.",
            ]),
        ],
    },
    {
        "id": "usage",
        "nav": "Commands and options",
        "title": "Commands and options",
        "description": "The mgit command line: the default behaviour, the parsing rule, the full option table, and how git options are handed through untouched.",
        "lede": "mgit only recognises the options in its own documentation. Everything else goes to git exactly as you typed it.",
        "blocks": [
            ("h2", "default", "The default"),
            ("p", "With no arguments, mgit runs `git status -s` in every repository:"),
            ("cmd", "mgit"),
            ("try", "workbench", "mgit", "Run it in the playground"),
            ("h2", "passthrough", "Any git command"),
            ("p", "Everything from the first argument that is not an mgit option is handed to git untouched:"),
            ("pre", "mgit pull\nmgit fetch --all --prune\nmgit log --oneline -n 3\nmgit checkout main\nmgit -c core.pager=cat status"),
            ("h2", "parsing", "The parsing rule"),
            ("p", "This is the only rule mgit asks you to remember. Everything after it is git's own behaviour:"),
            ("ol", [
                "Arguments are read left to right. At the first one that is **not an mgit option** (`pull`, `-c`, `--git-dir`, …), that argument and everything after it goes to git.",
                "A bare `--` does the same thing, even for words that mgit would otherwise claim.",
                "So the `--quiet` in `mgit log --quiet` belongs to git, and the `--quiet` in `mgit --quiet log` belongs to mgit.",
                "`mgit --version` prints mgit's version. To ask git for its own, use `mgit -- --version`.",
            ]),
            ("try", "workbench", "mgit -- --version", "Try mgit -- --version"),
            ("h2", "options", "Options"),
            ("table", None, ["Option", "What it does"], [
                ["`-h`, `--help`", "Print the help and exit"],
                ["`-V`, `--version`", "Print mgit's version and exit"],
                ["`-l`, `--list`", "List the repositories that were found, one absolute path per line, and exit"],
                ["`-d`, `--depth <N>`", "Search N directory levels down; the default is `1`, direct subdirectories only"],
                ["`-q`, `--quiet`", "Drop the per repository header and keep only git's own output"],
                ["`--color <WHEN>`", "`auto` (default), `always` or `never`"],
                ["`--no-color`", "The same as `--color=never`"],
                ["`--ascii`", "ASCII only glyphs, no emoji, for older terminals"],
                ["`--summary`", "Always print the closing summary, and print it to stdout"],
                ["`-k`, `--keep-going`", "Continue after a failing repository (the default)"],
                ["`--fail-fast`", "Stop at the first repository that fails"],
                ["`--allow-empty`", "Exit with `0` when no repository was found"],
                ["`--`", "Everything that follows goes to git"],
            ]),
            ("h2", "examples", "Examples"),
            ("table", None, ["Command", "What it is for"], [
                ["`mgit --list`", "See which directories would be touched, without running anything"],
                ["`mgit --depth 2 fetch`", "Reach repositories one level deeper as well"],
                ["`mgit -q log --oneline -n 1`", "Quiet mode, one line of history per repository"],
                ["`mgit --fail-fast pull`", "Stop at the first conflict instead of collecting them"],
                ["`mgit --color=never status`", "Turn colour off for a file or a CI log"],
                ["`mgit --ascii`", "Plain ASCII output for an older Windows console"],
            ]),
            ("try", "workbench", "mgit -q log --oneline -n 1", "Run it in the playground"),
        ],
    },
    {
        "id": "discovery",
        "nav": "How repositories are found",
        "title": "How repositories are found",
        "description": "The six rules behind mgit's search: depth, .git files and directories, never descending into a repository, symbolic links, ordering, and what happens when nothing is found.",
        "lede": "Six rules in total, and the result is identical on macOS, Linux and Windows.",
        "blocks": [
            ("h2", "rules", "The six rules"),
            ("ol", [
                "The search starts at the **current directory** and goes down one level by default; `--depth` reaches further.",
                "A directory counts as a repository when it holds a `.git` **directory** or a `.git` **file**, which is what makes `git worktree` and submodule checkouts work.",
                "Once a repository is found, mgit does **not** descend into it, so a submodule or a vendored project is never counted twice.",
                "Symbolic links are followed and reported under the name you can see: with `repo` and `link -> repo` side by side, both names are run. Each real directory is only entered once, so a link loop cannot spin forever.",
                "Results are sorted by name, case insensitively, with the exact spelling breaking ties, so the order is the same on all three operating systems.",
                "When nothing is found, mgit warns and exits with `1`. But if the **current directory itself** is a repository, mgit runs git right there and behaves exactly like plain git.",
            ]),
            ("try", "nested", "mgit --list", "Try it on a directory with a second level"),
            ("h2", "depth", "What --depth means"),
            ("p", "Levels are counted down from the current directory: `--depth 1`, the default, sees direct subdirectories, and `--depth 2` sees one more level. Because of rule 3, a larger depth never makes mgit walk into a repository it has already found."),
            ("try", "nested", "mgit --depth 2 --list", "Compare depth 1 and 2"),
            ("note", "Look before you touch", "Before any command that changes state, `mgit --list` tells you exactly which directories are involved."),
            ("h2", "worktree", "Worktrees and submodules"),
            ("p", "A directory created by `git worktree`, and the working directory of a submodule, keep their `.git` as a **file** rather than a directory. mgit accepts both, so both kinds of directory are found and run."),
            ("try", "worktrees", "mgit --list", "See a .git that is a file"),
            ("h2", "links", "Symbolic links"),
            ("p", "A link is reported under the name you see, deliberately matching the `*/` glob behaviour of the 1.0 shell version. Each real directory is entered only once, so even a directory linked back to its own parent finishes the walk."),
            ("try", "links", "mgit --depth 2 --list", "See what a link does"),
            ("h2", "unreadable", "A directory that cannot be read"),
            ("p", "A directory without read permission does not fail the run: mgit writes one warning to **stderr** and finishes the rest of the walk."),
            ("try", "locked", "mgit --depth 2 --summary", "See the warning, and the rest still running"),
            ("h2", "none", "Nothing found"),
            ("p", "When no repository is found, mgit prints a warning on stderr and exits with `1`. This is one of the defects 2.0 fixed on purpose: 1.0 was completely silent and returned `0`. `--allow-empty` brings the old exit code back."),
            ("try", "empty", "mgit --allow-empty", "Compare with and without --allow-empty"),
        ],
    },
    {
        "id": "output",
        "nav": "Output, colour and glyphs",
        "title": "Output, colour and glyphs",
        "description": "The mgit output format: the per repository header, the summary block, the colour policy, the ASCII fallback, and how stdout and stderr are separated.",
        "lede": "The format has not changed since 1.0, and colour is never the only thing carrying information.",
        "blocks": [
            ("h2", "header", "The per repository header"),
            ("p", "Each repository is introduced by a header framed with 80 `=` characters, carrying the directory name and the current branch. A detached HEAD shows the short commit instead."),
            ("pre", "================================================================================\n📂 Folder: web-console │ Branch: main\n================================================================================\n M src/main.rs"),
            ("p", "`-q` / `--quiet` drops the header and leaves git's own output, with a blank line still separating the repositories."),
            ("try", "workbench", "mgit --quiet", "Compare with and without --quiet"),
            ("h2", "summary", "The summary"),
            ("p", "When any repository failed, a summary is printed before mgit exits. `--summary` prints it every time."),
            ("pre", "────────────────────────────────────────────────────────────────────────────────\nmgit: 6 repositories, 5 succeeded, 1 failed\n  ✗ web-console (exit code 128)"),
            ("h2", "streams", "stdout and stderr"),
            ("p", "This is what makes mgit safe to put in a script: **the summary goes to stderr by default** and never pollutes stdout. `mgit status -s > status.txt` gives you clean data, and failures are still visible."),
            ("table", None, ["What", "Goes to"], [
                ["The per repository header", "stdout"],
                ["git's own output", "wherever git writes it; mgit does not intercept it"],
                ["The summary printed after a failure", "stderr"],
                ["The summary requested with `--summary`", "**stdout**"],
                ["The warnings about missing or unreadable directories", "stderr"],
                ["The paths printed by `--list`", "stdout"],
            ]),
            ("try", "workbench", "mgit --summary > status.txt", "Redirect it, then read the file back"),
            ("h2", "colour", "Colour"),
            ("p", "Colour only appears when the output is attached to a terminal that supports it. The precedence is: the command line, then `MGIT_*`, then `CLICOLOR_FORCE`, then `NO_COLOR` / `CLICOLOR`, then terminal detection."),
            ("table", None, ["Situation", "Result"], [
                ["Output redirected to a file or a pipe", "colour is off"],
                ["`NO_COLOR` set to a non empty value", "colour is off ([no-color.org](https://no-color.org/))"],
                ["`CLICOLOR_FORCE` set to something other than `0`", "colour is on"],
                ["`--color=always`", "always on, overriding everything above"],
                ["`--color=never`, `--no-color`", "always off, overriding everything above"],
            ]),
            ("try", "workbench", "NO_COLOR=1 mgit --summary", "Try NO_COLOR"),
            ("h2", "glyphs", "Glyphs and the ASCII fallback"),
            ("p", "The default glyphs are `📂`, `│`, `✗` and `─`. When the terminal cannot render them, mgit switches to plain ASCII: `Folder: … | Branch: …`, `x` and `-`."),
            ("ul", [
                "A Windows console whose output code page is not UTF-8 gets ASCII automatically.",
                "A POSIX locale that is not UTF-8 (`LANG=C`, `LC_ALL=POSIX`) gets ASCII automatically.",
                "`--ascii` forces ASCII, `MGIT_ASCII=1` does the same, and `MGIT_ASCII=0` forces the Unicode glyphs back.",
            ]),
            ("try", "workbench", "MGIT_ASCII=1 mgit --summary", "Try the ASCII glyphs"),
        ],
    },
    {
        "id": "exit-codes",
        "nav": "Exit codes and automation",
        "title": "Exit codes and automation",
        "description": "mgit's exit codes, how many repositories become one code, and how to use mgit from shell scripts, CI pipelines and pipes.",
        "lede": "Everything you need to know before a script decides something based on the result.",
        "blocks": [
            ("h2", "codes", "Exit codes"),
            ("table", None, ["Code", "Meaning"], [
                ["`0`", "every repository succeeded (or `--allow-empty` and nothing was found)"],
                ["`1`", "a repository failed, or no repository was found"],
                ["`2`", "the command line could not be parsed"],
                ["`127`", "`git` could not be found or started"],
                ["`130`", "interrupted with Ctrl+C (`SIGINT` / `SIGTERM`)"],
                ["anything else", "whatever the first failing git command returned"],
            ]),
            ("h2", "aggregation", "How many repositories become one code"),
            ("ol", [
                "By default the run **finishes everything** (`--keep-going`) instead of stopping at a failure.",
                "The overall exit code is the code of the **first** repository that failed, not the last.",
                "`--fail-fast` stops at the first failure, and the repositories it never reached are counted as skipped in the summary.",
                "Ctrl+C is not a repository failure: it stops the whole run at once and returns `130`.",
            ]),
            ("try", "fleet", "mgit --fail-fast pull", "Compare --fail-fast with the default"),
            ("h2", "interrupt", "Interrupting a run"),
            ("p", "Ctrl+C reaches the whole process group, which includes the git that mgit started. mgit treats that as \"stop now\": it does not move on to the next repository and it does not print a summary."),
            ("try", "fleet", "mgit pull", "Start it, then press Ctrl+C"),
            ("h2", "scripts", "In a shell script"),
            ("pre", "#!/usr/bin/env sh\nset -eu\n\n# fail when any repository has uncommitted work\nif [ -n \"$(mgit --quiet --color=never status --porcelain)\" ]; then\n  echo 'working trees are not clean' >&2\n  exit 1\nfi"),
            ("pre", "# update everything, then pick the failures out\nmgit pull 2> pull-errors.txt || {\n  echo 'some repositories failed:' >&2\n  grep '✗' pull-errors.txt >&2\n}"),
            ("h2", "ci", "In CI"),
            ("pre", "- name: Update every checkout\n  run: mgit --color=never --summary pull\n\n- name: Fail when anything is dirty\n  run: |\n    test -z \"$(mgit -q --color=never status --porcelain)\""),
            ("note", "CI logs", "A CI log is usually not a terminal, so colour is already off. Writing `--color=never` only makes that explicit."),
            ("h2", "pipes", "Pipes and SIGPIPE"),
            ("p", "`mgit --list | head -n 3` is safe: mgit restores the default `SIGPIPE` behaviour, so when the reader goes away it stops quietly instead of printing a panic into your log."),
            ("try", "fleet", "mgit --list | head -n 3", "Try it against head"),
        ],
    },
    {
        "id": "environment",
        "nav": "Environment and platforms",
        "title": "Environment and platforms",
        "description": "The environment variables mgit reads, their precedence, and how it behaves on macOS, Linux and the Windows consoles.",
        "lede": "The differences between terminals are absorbed by the tool instead of being pushed onto you.",
        "blocks": [
            ("h2", "variables", "Environment variables"),
            ("table", None, ["Variable", "What it does"], [
                ["`MGIT_GIT`", "The name or path of the git executable (default `git`)"],
                ["`MGIT_COLOR`", "`auto`, `always` or `never`; beats terminal detection"],
                ["`MGIT_ASCII`", "`1` forces the ASCII glyphs, `0` forces the Unicode ones"],
                ["`NO_COLOR`", "Disables colour when set to a non empty value"],
                ["`CLICOLOR_FORCE`", "Forces colour when set to something other than `0`"],
                ["`CLICOLOR`", "Disables colour when set to `0`"],
                ["`MGIT_VERSION`", "The version the installer should fetch, for example `2.0.0`"],
                ["`MGIT_INSTALL_DIR`", "The directory the installer should install into"],
            ]),
            ("p", "Precedence: the command line, then `MGIT_*`, then `CLICOLOR_FORCE`, then `NO_COLOR`, then terminal detection."),
            ("try", "workbench", "CLICOLOR_FORCE=1 NO_COLOR=1 mgit --summary", "Try the precedence"),
            ("h2", "platforms", "Platform behaviour"),
            ("table", None, ["Situation", "Behaviour"], [
                ["Output redirected (pipe, file, CI log)", "colour off, plain text only"],
                ["Windows `cmd.exe` / Windows PowerShell 5.1", "virtual terminal mode is enabled at startup so ANSI works; colour is disabled when it cannot be"],
                ["A Windows console whose code page is not UTF-8", "ASCII glyphs, so the emoji cannot turn into noise"],
                ["PowerShell 7, Windows Terminal, macOS, Linux", "the Unicode glyphs"],
                ["A POSIX locale that is not UTF-8 (`LANG=C`)", "ASCII glyphs; `MGIT_ASCII=0` forces Unicode back"],
                ["`mgit … | head`", "the default `SIGPIPE` behaviour is restored, so a closed pipe ends the run quietly"],
                ["`git` on Windows", "found through `PATH` (including `PATHEXT`); `MGIT_GIT` can point at a full path"],
            ]),
            ("p", "The \"where the output goes\" control in the playground switches between these situations, so you can watch the same command change."),
            ("try", "workbench", "mgit --summary", "Switch the output target and run it again"),
            ("h2", "missing-git", "When git is missing"),
            ("p", "mgit checks that git can be started before it does anything. When it cannot, mgit prints one error with the install hint for that platform and exits with `127`."),
            ("try", "workbench", "mgit", "Press \"Remove git\" first, then run it"),
        ],
    },
    {
        "id": "recipes",
        "nav": "Recipes",
        "title": "Recipes",
        "description": "Concrete ways to use mgit day to day: the morning check, looking only at repositories with changes, switching branches in bulk, and finding what is behind.",
        "lede": "Every block here can be pasted straight into a terminal, or tried in the playground first.",
        "blocks": [
            ("h2", "morning", "The morning check"),
            ("cmd", "mgit --list", "See which directories today's commands will touch."),
            ("cmd", "mgit", "The state of every repository at once."),
            ("cmd", "mgit pull", "Update everything; whatever failed is listed in the summary."),
            ("try", "workbench", "mgit pull", "Run it in the playground"),
            ("h2", "dirty", "Only the repositories with changes"),
            ("p", "`status --porcelain` prints nothing for a clean repository, so with `-q` only the repositories that have something to say are left:"),
            ("cmd", "mgit -q status --porcelain"),
            ("p", "Drop the `-q` when you need to know which repository each line came from: a clean one costs you a header and a blank line."),
            ("h2", "branches", "Switching branches in bulk"),
            ("cmd", "mgit branch --show-current", "See which branch each repository is on."),
            ("cmd", "mgit checkout main", "Put them all back on main; whatever could not switch shows up in the summary."),
            ("try", "workbench", "mgit branch --show-current", "Run it in the playground"),
            ("h2", "behind", "Finding what is behind"),
            ("cmd", "mgit fetch --all --prune", "Refresh the remote information; this touches no working tree."),
            ("cmd", "mgit -q log --oneline -n 1", "Then look at the newest commit in each repository."),
            ("h2", "nested", "A working directory with two levels"),
            ("p", "When the repositories live under `work/team-a/*` and `work/team-b/*`, reach one level further:"),
            ("cmd", "mgit --depth 2 --summary"),
            ("try", "nested", "mgit --depth 2 --summary", "Try it on a nested directory"),
            ("h2", "not", "When not to use mgit"),
            ("ul", [
                "When you need to **pin** the exact version of each dependency: that is what `git submodule` is for.",
                "When you need **one CI pipeline and one release**: that is a monorepo question, not a multi repository one.",
                "When you need an atomic commit across repositories: mgit sends one command to each directory and offers no transaction of any kind.",
            ]),
            ("p", "mgit puts no files inside your repositories and changes no relationship between them. It does one thing: send the same command to every repository, then collect the results into one exit code."),
        ],
    },
]

PLAYGROUND = {
    "title": "mgit playground — run mgit in your browser",
    "description": "Try mgit in a browser sandbox: an editable working directory, the real argument parsing and discovery rules, a run you can watch repository by repository, and the reason behind every exit code. Your files are never touched.",
    "h1": "Playground",
    "lede": "A make believe working directory on the left, a terminal in the middle, and what mgit decided on the right. All of it lives in this tab and none of it can reach the files on your machine.",
    "fidelity_title": "How real is this?",
    "fidelity": [
        "**Argument parsing, repository discovery, the output format and the exit codes** are ported rule by rule from mgit's Rust source, and a parity test keeps them honest: the same working directory and the same command are given to the compiled binary and to this code, and the output has to match character for character.",
        "**git itself is simulated.** The sandbox knows what `status`, `pull`, `fetch`, `log`, `branch` and `checkout` should answer for these make believe repositories. For a git command it does not model, it says so instead of inventing output.",
        "Sandbox paths are always POSIX style. Switching to a Windows output target changes **the glyph and colour decisions**, not the path format.",
    ],
}
